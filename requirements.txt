# What app.py actually imports, plus the WSGI server the Procfile starts.
#
# Note that Tesseract and poppler are system binaries and are NOT installable
# from here — see the README for the apt/brew lines.
Flask==1.0.3
pytesseract==0.3.7
Pillow==7.0.0
pdf2image==1.14.0
gunicorn==19.9.0
